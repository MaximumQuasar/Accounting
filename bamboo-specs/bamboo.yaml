project:
  key: AC
  plan:
    key: YAC
    name: Default Job

stages:
  - jobs:
      - scripts:
          - ${bamboo.capability.system.builder.command.Nuget} restore
      - scripts:
          - '#!/bin/bash'
          - ${bamboo_capability_system_builder_msbuild_MSBuild_v4_0_64bit}
          interpreter: shell
