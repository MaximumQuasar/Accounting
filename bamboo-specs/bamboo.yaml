project:
  key: AC
  plan:
    key: YAC
    name: Default Job

stages:
  - jobs:
      - scripts:
          - ${bamboo.capability.system.builder.command.Nuget} restore
      - scripts:
          - ${bamboo.capability.system.builder.msbuild.MSBuild} Accounting.sln /p:configuration=Release /p:target="Any CPU" /p:PublishProfile=Accounting.pubxml /p:DeployOnBuild=true
