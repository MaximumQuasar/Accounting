project:
  key: AC
  plan:
    key: YAC
    name: Default Job

stages:
  - jobs:
      - scripts:
          - ${bamboo.capability.system.builder.command.Nuget} restore
      - scripts:
          -  ${bamboo.capability.system.msbuild.MSBuild_v4_0_64_bit}
